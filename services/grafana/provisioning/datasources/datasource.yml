apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: energy-postgres
    url: database:5432
    database: postgres
    user: postgres
    secureJsonData:
      password: postgres
    jsonData:
      sslmode: disable
      postgresVersion: 1400
      timescaledb: false
